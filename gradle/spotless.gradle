apply plugin: "com.diffplug.gradle.spotless"

spotless {
    java {
        removeUnusedImports()
        googleJavaFormat()
    }
    groovyGradle {
        target '**/*.gradle'
        indentWithSpaces()
    }
    // freshmark does not work on Java 9
    // freshmark { target 'README.md'}
}
